# AgentProof PR fixture

`calculator.py` is the final repository state committed by the canonical Codex session
receipt in `.agentproof/agent-session.json`. The pull request Action hashes this file on
the runner before it seals or anchors anything.
